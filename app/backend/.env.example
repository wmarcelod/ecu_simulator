# Server Configuration
PORT=8000
HOST=0.0.0.0
NODE_ENV=development

# Database
# Database file is stored at data/simulator.db by default

# CORS
CORS_ORIGIN=http://localhost:3000

# WebSocket Configuration
WS_HEARTBEAT_INTERVAL=30000
WS_MAX_CONNECTIONS=10
